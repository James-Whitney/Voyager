#include "Hud.h"
#include <imgui/imgui.h>
#include <imgui/imgui_impl_glfw_gl3.h>

/*
Hud::Hud(GLFWwindow* window) {
  ImGui_ImplGlfwGL3_Init(window, true);
}

void Hud::start() {
  ImGui_ImplGlfwGL3_NewFrame();
}

void Hud::render() {
  ImGui::Render();
}

void Hud::textbox(std::string txt, int x_pos, int y_pos, int height, int width) {
  ImGui::SetNextWindowPos(ImVec2(x_pos, y_pos), 0, ImVec2(0.5f,0.5f));
  ImGui::SetNextWindowSize(ImVec2(height, width), 0);

  ImGui::Begin(title, NULL, beginDefaults);
  ImGui::Text(txt);
  ImGui::End();
}
*/
